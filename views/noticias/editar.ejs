<%- include ('../partials/header.ejs') %>

   
    <section id="about">
      <div class="container">
        <div class="row">
          <div class="col-lg-8 mx-auto">
            <form method="post">
            <h2>Editar Notícia</h2>
            <div class="form-group">
              <label for="name">Título da Notícia</label>
              <input value="<%= noticia.title %>" name="name" type="text" class="form-control" id="exampleInputEmail1" aria-describedby="nameHelp" placeholder="Informe Título">
              <small id="nameHelp" class="form-text text-muted">Título da Notícia a ser editada</small>
              <% if(errors && errors.indexOf('titulo')>=0) { %>
                <small id="nameHelp" class="form-text text-muted">POR FAVOR, INFORME O TÍTULO DA NOTICIA!!!</small>
              <% } %>
            </div> 
            <div class="form-group">
                <label for="status">Situação:</label>
              <select class="form-control" id="status" name="status">
                <% labels.forEach(label => { %>
                <option value="<%= label.id %>" <% if(label.id === noticia.status) { %> selected="selected" <% } %>><%= label.title %></option>
                <% }) %>
               </select>
            </div>

            <button class="btn btn-primary">Salvar</button>
          </form>
          </div>
        </div>
      </div>
    </section>

    <%- include ('../partials/footer.ejs') %>
